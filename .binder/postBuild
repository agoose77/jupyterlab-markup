#!/usr/bin/env bash
set -eux

python -m pip install -e . --no-deps --ignore-installed
jupyter serverextension enable jupyterlab_markup --sys-prefix
jupyter serverextension list

jlpm --ignore-optional
jupyter labextension link . --no-build --debug
jupyter lab build --debug --minimize=True --dev-build=False
jupyter labextension list
